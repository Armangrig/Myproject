name: My Github Actions

on:
 
  push:
    branches: 
     - main
      
 
jobs:
  if-Windows-else:
    runs-on: macos-12
   
    steps:

       - id: 1ww
         run: echo "AA=Hello" >> $GITHUB_OUTPUT

       - id: 2ww
         run: echo "BB=world" >> $GITHUB_OUTPUT



       - run: echo ${{ steps.1ww.outputs.AA }} ${{ steps.2ww.outputs.BB }}
   
